import { AccountOperationTypePipe } from './account-operation-type.pipe';

describe('AccountOperationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountOperationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
