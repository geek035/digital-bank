import { CardOperationTipePipe } from './card-operation-tipe.pipe';

describe('CardOperationTipePipe', () => {
  it('create an instance', () => {
    const pipe = new CardOperationTipePipe();
    expect(pipe).toBeTruthy();
  });
});
