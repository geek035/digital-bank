import { CardTermClassPipe } from './card-term-class.pipe';

describe('CardTermClassPipe', () => {
  it('create an instance', () => {
    const pipe = new CardTermClassPipe();
    expect(pipe).toBeTruthy();
  });
});
