import { ProgramTypeLogoPipe } from './program-type-logo.pipe';

describe('ProgramTypeLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramTypeLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
