import { CardStatePipe } from './card-state.pipe';

describe('CardStatePipe', () => {
  it('create an instance', () => {
    const pipe = new CardStatePipe();
    expect(pipe).toBeTruthy();
  });
});
