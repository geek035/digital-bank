import { AccountStatePipe } from './account-state.pipe';

describe('AccountStatePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountStatePipe();
    expect(pipe).toBeTruthy();
  });
});
